name: provider_todo_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^0.14.0+3
  flutter_state_notifier: ^0.7.0
  freezed_annotation: ^0.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: ^1.14.0
  build_runner: ^2.0.6
  freezed: ^0.14.2

flutter:

  uses-material-design: true

